## Introduction

This module gives the ability to integrate a support chat only in all our plugin setting pages.

## Installation

* Run composer `require woosa/support-chat:<version>` in the plugin's root directory. Alternatively, you can add `"woosa/support-chat": "<version>"` directly in `composer.json` and then run `npm start`

